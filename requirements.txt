# Dependencies for Hugging Face Spaces — installed by the Space builder (not pinned to CUDA wheels;
# Spaces selects CPU/GPU-compatible torch builds automatically).
numpy>=1.26,<3
torch>=2.1,<3
transformers>=4.40,<5
gradio>=4.44,<6
python-docx>=1.1.0
pdfplumber>=0.11.0
rank-bm25>=0.2.2
